var config = {
    jwtCode: {
        api_secret_key : "so secret"
    },
    mysqlInfo: {
        connectTimeout: 10,
        password: '',
        user: 'root',
        database: 'test',
        host: '127.0.0.1',
        port: 3306
    },
    models:[
    ]
}

module.exports = config;